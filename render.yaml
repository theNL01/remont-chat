services:
  - type: web
    name: remont-chat
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python chat_server.py
    envVars:
      - key: PORT
        value: 10000
